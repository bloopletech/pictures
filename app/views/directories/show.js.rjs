page.insert_html(:bottom, "items", :partial => 'item', :collection => @items)
page << "update_items()"
if is_last_page?(@items)
  page << "last_page = true;"
  page.insert_html(:bottom, "items", :partial => 'end_of_results')
end
